<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@android:id/background">
        <shape>
            <corners android:radius="10dip" />
            <gradient
                android:startColor="@color/colorBackgroundBar"
                android:centerColor="@color/colorBackgroundBar"
                android:endColor="@color/colorBackgroundBar"
                />
        </shape>
    </item>


    <item
        android:id="@android:id/progress"
        >
        <clip>
            <shape>
                <corners
                    android:radius="10dip" />
                <gradient
                    android:startColor="@color/colorForegroundBar"
                    android:endColor="@color/colorForegroundBar" />
            </shape>
        </clip>
    </item>

</layer-list>
